 <view class="recommend">
   <block wx:for="{{images}}" wx:key="index" >
      <view class="recommend-item"  bindtap="toDetailsTap" data-id="{{item.id}}" >
        <image src="{{item.picUrl}}"></image>
            <text>{{item.title}}</text>
      </view>
   </block>
 </view>

